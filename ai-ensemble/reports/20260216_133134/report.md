# AI Code Review Report
**Date:** Mon Feb 16 13:32:31 KST 2026
**Context:** 파일 plan_request.md 리뷰
**Prompt Hash:** `1eeadf1e2c9b5e88e19cfb72ad683a81b6984f3d30b5e5540a168867d70910d8`

## Status


## Claude Response
# AI 뉴스 수집기 리뷰

## 요약
`plan_request.md` 파일에 작성된 프로젝트 요구사항은 AI 기반 뉴스 수집 시스템의 **초기 컨셉**으로서 방향성은 명확하나, 실제 구현을 위해서는 **구체성이 부족**합니다. 기획 문서로서 필수 요소(사용자 스토리, 비기능 요구사항, 제약사항)가 누락되어 있으며, 기술 스택과 기능 설명이 너무 추상적입니다.

---

## 개선항목

### 1. **정확성(Correctness)** - 요구사항 명확화 필요

**문제점:**
- "RSS 피드 수집, 요약, 태깅"이 구체적으로 무엇을 의미하는지 불분명합니다.
  - 수집 대상 RSS 소스는? (고정 목록 vs 사용자 추가 가능)
  - 요약 방식은? (LLM 기반 vs 추출적 요약)
  - 태깅 기준은? (자동 분류 vs 사용자 정의 태그)

**해결 방안:**
```markdown
## 핵심 기능 (User Stories)
1. **RSS 수집**: 관리자가 설정한 N개 RSS 피드를 매 시간 크롤링하여 신규 기사를 수집
2. **AI 요약**: Claude API를 사용하여 기사 본문을 3줄 요약 생성 (원문 길이 > 500자)
3. **자동 태깅**: 기사 내용을 분석하여 `정치`, `경제`, `기술` 등 카테고리 자동 분류
4. **검색 및 필터**: 사용자가 태그/키워드로 기사 검색 가능
```

---

### 2. **스타일(Style)** - 기술 스택 선정 근거 부재

**문제점:**
- "Python, FastAPI, React"를 왜 선택했는지 이유가 없습니다.
- 대안 기술(예: Django vs FastAPI, Vue vs React)을 검토했는지 알 수 없습니다.

**해결 방안:**
```markdown
## 기술 스택 선정 이유
- **Backend (FastAPI)**: 비동기 RSS 크롤링에 적합, Pydantic 기반 타입 안전성
- **Frontend (React)**: 대시보드 UI 컴포넌트 재사용성 높음
- **AI (Claude API)**: 요약 품질 우수, 한국어 지원
- **DB (PostgreSQL + pgvector)**: 기사 유사도 검색을 위한 벡터 DB 필요
```

---

### 3. **보안(Security)** - 인증/권한 관리 누락

**문제점:**
- 누가 RSS 소스를 추가/삭제할 수 있는지 명시되지 않았습니다.
- API 엔드포인트에 대한 접근 제어 정책이 없습니다.

**해결 방안:**
```markdown
## 보안 요구사항
- **인증**: JWT 기반 로그인 (관리자/일반 사용자 구분)
- **권한**: 
  - 관리자만 RSS 소스 추가/삭제 가능
  - 일반 사용자는 읽기 전용
- **API Rate Limiting**: Cloudflare / FastAPI의 `slowapi` 적용
```

---

### 4. **성능(Performance)** - 확장성 고려 부족

**문제점:**
- RSS 피드가 100개 이상으로 늘어나면 크롤링 시간이 급증할 수 있습니다.
- 요약 생성 시 Claude API 호출 비용/속도 제한을 고려하지 않았습니다.

**해결 방안:**
```markdown
## 성능 최적화 전략
1. **비동기 크롤링**: `httpx.AsyncClient`로 병렬 요청 (동시 처리 20개)
2. **캐싱**: Redis를 사용하여 이미 수집한 기사는 재요약 방지
3. **백그라운드 작업**: Celery + RabbitMQ로 요약 작업 큐 처리
4. **페이지네이션**: 기사 목록 API는 50개 단위로 반환
```

---

### 5. **추가 누락 사항**

#### (1) 비기능 요구사항
```markdown
## 비기능 요구사항
- **응답 시간**: 기사 목록 API는 300ms 이내 응답
- **가용성**: 99.5% 업타임 (월 3.6시간 다운타임 허용)
- **확장성**: RSS 소스 1,000개까지 지원
```

#### (2) 데이터 스키마 힌트
```markdown
## 예상 DB 스키마 (초안)
- **rss_sources**: id, url, name, category, is_active
- **articles**: id, source_id, title, url, content, summary, published_at
- **tags**: id, name
- **article_tags**: article_id, tag_id (다대다 관계)
```

#### (3) API 명세 예시
```markdown
## 주요 API 엔드포인트
- `POST /api/sources` - RSS 소스 추가 (관리자 전용)
- `GET /api/articles?tag=기술&limit=50` - 기사 목록 조회
- `GET /api/articles/{id}` - 기사 상세 + 요약
- `POST /api/articles/{id}/summarize` - 수동 재요약 트리거
```

---

## 최종 판정

**❌ 현재 상태로는 구현 불가**

이 문서는 **아이디어 스케치** 수준이며, 다음 단계로 넘어가기 위해 필요한 사항:

1. **사용자 스토리 구체화** (위 예시 참고)
2. **기술 선정 근거 추가**
3. **보안/성능 요구사항 명시**
4. **DB 스키마 초안 작성**
5. **API 명세 초안 작성**

위 5가지를 보완한 후 다시 리뷰를 받으면 **"LGTM"** 판정을 받을 수 있습니다.

---

`★ Insight ─────────────────────────────────────`
**기획 문서 작성 시 핵심 원칙:**
1. **5W1H 원칙**: 누가(Who), 무엇을(What), 언제(When), 어디서(Where), 왜(Why), 어떻게(How)를 모두 답해야 합니다.
2. **SMART 목표**: 구체적(Specific), 측정 가능(Measurable), 달성 가능(Achievable), 관련성(Relevant), 시한(Time-bound)을 고려해야 합니다.
3. **기술 부채 예방**: 초기 기획 단계에서 보안/성능/확장성을 명시하지 않으면, 나중에 리팩토링 비용이 10배 이상 증가합니다.
`─────────────────────────────────────────────────`

---

## Codex Response
**요약**
`plan_request.md`는 방향성은 맞지만, 구현 계획(DB 스키마/API 명세)을 정확하게 뽑기엔 요구사항이 너무 추상적입니다. 현재 상태로는 정확성·보안·성능 측면에서 설계 편차가 크게 날 가능성이 높습니다.

**개선항목**
1. `[높음][정확성]` `plan_request.md:3`, `plan_request.md:4`, `plan_request.md:7`  
요구사항이 기술/기능 키워드 수준이라 DB/API를 일관되게 설계하기 어렵습니다.  
해결: 엔티티(Feed, Article, Summary, Tag), 필수 필드, 고유키(예: `source_id+guid`), 중복 처리 규칙, 요약 길이/언어 규칙을 명시하세요.

2. `[높음][보안]` `plan_request.md:4`, `plan_request.md:7`  
RSS 수집 기능은 SSRF, 악성 콘텐츠 유입 위험이 큽니다(특히 사용자 입력 URL 허용 시).  
해결: URL 검증, 사설망/localhost 차단, 요청 타임아웃·크기 제한, HTML/스크립트 정제 정책을 요구사항에 포함하세요.

3. `[중간][성능]` `plan_request.md:4`, `plan_request.md:7`  
처리량 기준이 없어 수집/요약을 동기 처리로 설계할 위험이 있습니다.  
해결: 예상 피드 수/수집 주기/SLA를 추가하고, 비동기 큐(수집-요약 분리), 배치 처리, 인덱스 전략을 요구하세요.

4. `[중간][스타일]` `plan_request.md:7`  
요청 산출물 포맷이 모호해 결과물 품질이 들쭉날쭉할 수 있습니다.  
해결: “OpenAPI 3.1(YAML), ERD, SQL DDL, 주요 API 예시 요청/응답, 에러 코드 표”처럼 출력 형식을 고정하세요.

5. `[중간][정확성/운영]` `plan_request.md:4`  
데이터 생명주기(갱신/삭제/보관) 요구가 없습니다.  
해결: 재수집 충돌 정책, 보관 기간, 소프트 삭제 여부, 태그 재분류 정책을 정의하세요.

6. `[낮음][보안/운영]` `plan_request.md:7`  
인증/인가, 레이트 리밋, 감사 로그 요구가 빠져 있습니다.  
해결: 인증 방식(JWT/OAuth2), 권한 모델, API rate limit, 모니터링/알림 기준을 추가하세요.

---

## Gemini Response
### 요약

이 문서는 `Python/FastAPI`, `React`를 사용하여 `AI 뉴스 수집기`를 개발하기 위한 상위 수준의 요구사항을 담고 있습니다. 주요 기능으로 RSS 피드 수집, 기사 요약, 태깅을 정의하고, 이를 바탕으로 상세 구현 계획(DB 스키마, API 명세) 수립을 요청하고 있습니다.

요구사항이 명확하여 이를 바탕으로 상세 계획을 수립하기에 충분합니다. 다만, 실제 구현에 들어가기 전 몇 가지 구체화하면 좋을 점들이 있어 아래에 제안합니다.

### 개선항목

- **[명확성] 요구사항 구체화**
  - **RSS 피드 관리**: 피드를 사용자가 직접 추가/관리하는 형태인가요, 아니면 관리자가 미리 정해진 피드 목록을 제공하는 형태인가요? 피드 수집 주기(예: 1시간, 6시간)는 어떻게 설정할 계획인지 명확히 하면 좋습니다.
  - **AI 모델 선정**: 기사 '요약'과 '태깅'에 사용할 구체적인 AI 모델(예: `gpt-3.5-turbo`, `gemini-pro`)이 무엇인가요? 모델에 따라 API 요청/응답 형식과 비용이 달라지므로 초기 설계에 중요한 요소입니다.

- **[설계] 구현 시 고려사항**
  - **성능**: 다수의 RSS 피드를 읽고 AI 모델로 처리하는 작업은 시간이 오래 걸릴 수 있습니다. FastAPI의 `BackgroundTasks`나 `Celery` 같은 작업 큐를 활용하여 이들을 비동기 백그라운드 작업으로 처리하면, API 응답 지연을 막고 더 나은 사용자 경험을 제공할 수 있습니다.
  - **보안**: 외부 AI 서비스 연동에 필요한 API 키는 소스 코드에 직접 하드코딩하지 말고, `.env` 파일과 같은 환경 변수를 통해 안전하게 관리하는 것을 권장합니다.
  - **데이터 관리**: 기사 원문, 요약문, 태그 등을 저장할 데이터베이스로 초기에는 `SQLite`가 편리하지만, 향후 확장성을 고려하여 `PostgreSQL` 같은 보다 강력한 RDBMS 사용도 고려해볼 수 있습니다.

위 사항들은 현재 요구사항의 결함이라기보다는, 다음 단계인 **상세 구현 계획을 수립할 때 논의하고 결정하면 좋을 제안**들입니다. 현재 작성된 요구사항 문서는 프로젝트의 목표를 이해하기에 충분하며, 훌륭하게 작성되었습니다.
